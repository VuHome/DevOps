# Terraform
terraform/.terraform/
terraform/terraform.tfstate
terraform/terraform.tfstate.backup
terraform/*.tfplan
terraform/*.tfvars
terraform/override.tf
terraform/override.tf.json

# Secrets / local data
*.txt
*.env
.env*

# Cloudflare credentials - never commit (CF auto-revokes exposed tokens)
cf-data.txt
*-data.txt
*.secret
